Assignment Explain
1.ทำโปรแกรมแล้วชื่อไฟล์ test_web_crawler โดยจะดึงไฟล์จาก rss ของ bbc
2.อยู่ใน crontab_script.txt
3.วิธีที่ป้องกันไม่ให้ดึงข่าวซ้ำคือจะอ่านไฟล์เก่าก่อนว่ามีไหม ถ้ามีให้เอา link ทั้งหมดมาทำเป็น set แล้วนำ set มาเช็คก่อนว่าดึงไปหรือยัง ถ้ายังไม่ดึงถึงค่อยเอามารัน
4.เก็บไว้แล้วครับใน folder ชื่อ test.csv
5.ตามหัวข้อ 1 2 4 แล้วมีไฟล์ read_me.txt เป็นวิธีการรัน  Gitub:https://github.com/wisdonbond/solf_nix_test 
